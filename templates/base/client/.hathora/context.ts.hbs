import React from "react";
import { UserData } from "./base";
import { HathoraConnection } from "./client";
import { {{userState}} as UserState } from "./types";

type RtagContext = { connection: HathoraConnection; user: UserData; state: UserState; updatedAt: number } | undefined;
export const RtagContext = React.createContext<RtagContext>(undefined);
