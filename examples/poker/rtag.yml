types:
  Username: string
  PlayerStatus:
    - WAITING
    - FOLDED
    - PLAYED
  Card:
    rank: string
    suit: string
  PlayerInfo:
    name: Username
    chipCount: int
    chipsInPot: int
    cards: Card[]
    status: PlayerStatus
  PlayerState:
    players: PlayerInfo[]
    dealer: Username
    activePlayer: Username
    revealedCards: Card[]

methods:
  createGame:
    startingChips: int
    startingBlind: int
  joinGame:
  startRound:
  fold:
  call:
  raise:
    amount: int

auth:
  anonymous:
    separator: "-"
  
userState: PlayerState
initialize: createGame
error: string
