export type UserId = string;
{{#each types}}
{{#if (isArray this)}}
export enum {{@key}} {
  {{#join this ",
  " false false}}{{name}}{{/join}}
}
{{else if (isObject this)}}
export interface {{@key}} {
{{#join this ";
" false true}}  {{name}}: {{type}}{{/join~}}
}
{{else}}
export type {{@key}} = {{this}};
{{/if}}
{{/each}}

{{#each methods}}
{{#if (isArray this)}}
export enum {{makeRequestName @key}} {
  {{#join this ",
  " false false}}{{name}}{{/join}}
}
{{else if (isObject this)}}
export interface {{makeRequestName @key}} {
{{#join this ";
" false true}}  {{name}}: {{type}}{{/join~}}
}
{{else}}
export type {{makeRequestName @key}} = {{this}};
{{/if}}
{{/each}}
